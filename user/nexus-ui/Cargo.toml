[package]
name = "nexus-ui"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "NexusUI: drawing, text and layout for programs that have a surface."

[lib]
name = "nexus_ui"
path = "src/lib.rs"

[dependencies]
nexus-font = { path = "../../shared/nexus-font" }
