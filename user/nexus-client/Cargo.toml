[package]
name = "nexus-client"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "A program that draws into a surface a compositor gave it."

[[bin]]
name = "nexus-client"
path = "src/main.rs"
test = false

[dependencies]
nexus-user = { path = "../nexus-user" }
