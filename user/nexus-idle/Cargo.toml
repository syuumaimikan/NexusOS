[package]
name = "nexus-idle"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "A program that waits forever, so that stopping one can be tested."

[[bin]]
name = "nexus-idle"
path = "src/main.rs"
test = false

[dependencies]
nexus-user = { path = "../nexus-user" }
