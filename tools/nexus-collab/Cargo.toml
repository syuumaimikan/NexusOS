[package]
name = "nexus-collab"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Reads and writes the shared state two AI developers keep in .ai_collaboration."

[[bin]]
name = "nexus-collab"
path = "src/main.rs"

[dependencies]
nexus-json = { path = "../../shared/nexus-json" }
