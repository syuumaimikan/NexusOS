[package]
name = "nexus-probe"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "A Linux program that asks the machine which system calls it does not have."

[[bin]]
name = "nexus-probe"
path = "src/main.rs"
