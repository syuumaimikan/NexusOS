[package]
name = "nexus-abi"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Shared ABI types for NexusOS: boot handoff, memory map, syscall numbers."

[lib]
path = "src/lib.rs"

[dependencies]
