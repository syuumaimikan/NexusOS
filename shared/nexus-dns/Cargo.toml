[package]
name = "nexus-dns"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Turning a name into an address: the message format, and what to make of an answer."

[lib]
name = "nexus_dns"
path = "src/lib.rs"
