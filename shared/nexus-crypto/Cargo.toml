[package]
name = "nexus-crypto"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "SHA-512 and Ed25519, so that a package can be signed and a signature checked."

[lib]
name = "nexus_crypto"
path = "src/lib.rs"
