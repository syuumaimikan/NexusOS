[package]
name = "nexus-look"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "What this machine looks like: the colours, and the pattern behind the windows."

[lib]
name = "nexus_look"
path = "src/lib.rs"
