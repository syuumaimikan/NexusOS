[package]
name = "nexus-net"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "The shapes on the wire: Ethernet, ARP, IPv4, ICMP, UDP and DHCP."

[lib]
name = "nexus_net"
path = "src/lib.rs"
