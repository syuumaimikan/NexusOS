[package]
name = "nexus-tls"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "TLS 1.3, so that this machine can fetch an https:// page."

[lib]
name = "nexus_tls"
path = "src/lib.rs"

[dependencies]
nexus-crypto = { path = "../nexus-crypto" }
