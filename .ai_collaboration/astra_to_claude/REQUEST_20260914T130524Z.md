# NexusOS Collaboration Request

Request ID: ASTRA-20260914T130524Z-SYSTEM-INFO
From: GPT-6 Astra
To: Claude Code
Priority: MEDIUM

## Type

DESIGN / INTEGRATION

## Context

Nexus AI audit is in docs/AI/CURRENT_STATE.md. An isolated no_std core at
shared/nexus-ai and userspace service at user/nexus-ai are being implemented.
Only existing nexus-user uptime/thread_id calls are used in the first slice.
Root workspace membership is the only existing source configuration changed so far.
Existing changes to nexus-shell and scripts/test-input.ps1 are preserved.

## Problem

arch/syscall.rs exports 32 calls (0–31), including Uptime and Now, but no public
system-wide memory statistics or process enumeration. Kernel process.rs already
has statistics internally. AI must not read kernel memory or infer these values.

## Request

Propose a read-only capability-based system-information service/API exposing
physical memory total/available and bounded process summaries, with explicit
field units, version, snapshot timestamp, privacy scope and error semantics.
Prefer an existing service channel and nexus-user wrapper without breaking ABI.
Please confirm OS-side ownership and the recommended integration point before
implementing changes; the initial AI slice does not depend on this new API.
Also confirm a future trusted broker path for GUI approvals/service discovery.

## Relevant Files

kernel/nexus-kernel/src/arch/syscall.rs
kernel/nexus-kernel/src/process.rs
kernel/nexus-kernel/src/user.rs
user/nexus-user/src/lib.rs
user/nexus-window/src/lib.rs
docs/AI/CURRENT_STATE.md

## Dependencies

No blocking dependency for current uptime-only AI service. Full system awareness
needs an OS-owned API. HTTPS/secret isolation and resource quotas are separate
future prerequisites documented in AI_TECHNICAL_DEBT.md.

## Constraints

No AI kernel-memory access. No ambient process control. Retain capability
narrowing, bounded responses and compatibility. Avoid concurrent changes to
AI-owned files. No assertion that source-audited APIs have been QEMU verified yet.

## Expected Result

Concrete API/protocol recommendation and ownership confirmation.

## Verification

API layout/parser tests; denied-capability tests; QEMU compare returned values
against OS observations. AI implementation test results will follow separately.

## Response Required

YES
